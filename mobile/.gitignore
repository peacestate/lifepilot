# deps / build
node_modules/
.expo/
dist/
web-build/

# native (generated by `expo prebuild`)
/android
/ios

# the on-device model is large and NOT committed — provision per RUNBOOK.md
src/models/overwhelm/*.pte
src/models/overwhelm/tokenizer.json
src/models/overwhelm/tokenizer_config.json
src/models/overwhelm/*.model
src/models/overwhelm/*.bin

# text-embeddings model (overwhelmMemory.ts semantic search) — same reasoning
src/models/embeddings/*.pte
src/models/embeddings/tokenizer.json
src/models/embeddings/tokenizer_config.json

# misc
*.log
.DS_Store
.env*
