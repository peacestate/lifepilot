# OCR Expense — Field-Extraction Report

Date: {{DATE}} · Extractor: deterministic parser (no model) · Generated by `expense_eval.py`

## Verdict: {{VERDICT}}
_(PASS = every fixture matches the contract on total, date, merchant, category, and needsReview.)_

## Summary
| Field | Correct | Target |
|---|---|---|
| Total (amount + currency) | {{TOTAL_OK}} | all |
| Date (ISO) | {{DATE_OK}} | all |
| Merchant | {{MERCH_OK}} | all |
| Category | {{CAT_OK}} | all |
| needsReview flag | {{REVIEW_OK}} | all |

> OCR (image → text) is platform-native (Apple Vision / Google ML Kit), on-device, no upload —
> it is NOT exercised here. This harness validates the **field-extraction parser** over OCR
> text fixtures. The TypeScript port in `mobile/src/features/expense/` must reproduce these
> outputs number-for-number (run this in CI against both).

## Per-fixture results
_(confidence in parentheses; review = whether the field set is flagged for a human glance)_

| Fixture | Merchant | Total | Date | Category | Review | OK |
|---|---|---|---|---|---|---|
{{TABLE}}

## Notes
- _Receipts that needed review and why:_
- _Coefficient/keyword tweaks made:_
- _Real-device OCR quality observations (Vision vs ML Kit):_

## Sign-off
- [ ] AI/ML: parser matches contract; categories + thresholds reviewed
- [ ] CTO: native OCR path confirmed on both platforms; no network in capture/OCR/extract
- [ ] Mobile: TS port reproduces `expense_eval.py` outputs in CI
