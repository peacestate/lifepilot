# Hydration Tracker — Engine Report

Date: {{DATE}} · Engine: deterministic rules engine (NO model / NO `.pte`) · Generated by `hydration_eval.py`

This validates the reference implementation in `hydration_eval.py` against
`docs/hydration-engine-contract.md` (§1 formula, §4 output, §5 nudges). The TS port in
`mobile/src/features/hydration/` must reproduce these numbers.

## Verdict: {{VERDICT}}
_(PASS = every scenario in [FLOOR,CEILING], breakdown sums to target, per-scenario bounds &
status met, all relational + nudge assertions pass. Else REVIEW.)_

## Summary
| Check | Result | Target |
|---|---|---|
| Target in `[1500, 4000]` mL | {{RANGE_OK}} | all |
| Breakdown sums to target (±0.5 mL) | {{SUMS_OK}} | all |
| Per-scenario bounds + status met | {{BOUNDS_OK}} | all |
| Relational sanity (hot>cool, workout>rest, …) | {{REL_OK}} | all |
| Nudge logic (quiet/behind/ahead/spike/debounce) | {{NUDGE_OK}} | all |

## Per-scenario results
| Scenario | Target mL | Status | Baseline | Heat | Activity | AQI | Clamp | Conf | Bounds | Expectation |
|---|---|---|---|---|---|---|---|---|---|---|
{{TABLE}}

## Observations
- _Any scenario outside its expected band or wrong status:_
- _Clamp behaviour (floor/ceiling) hit where expected:_
- _Relational ordering results:_
- _Nudge scenario notes:_

## Sign-off
- [ ] AI/ML: formula §1 + bounds met; reference harness green; **no `.pte` by design (§0)**
- [ ] CTO: engine lives in `mobile/src/features/hydration/`, single frozen constants object, profile state local-only, TS matches this reference (contract §10)
- [ ] Mobile: `computeTarget` + `decideNudge` ported; mL internally, unit conversion at display
- [ ] Designer: "why today" panel binds `breakdown[]` (sums to target) + `status` headline + disclaimer
