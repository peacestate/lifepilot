# Overwhelm Manager — 20-Task Model Report

Date: {{DATE}} · Model: Llama 3.2 1B Instruct INT4 · Generated by `overwhelm_eval.py`

## Verdict: {{VERDICT}}
_(PASS = ≥18/20 on both step-count and format. Otherwise REVIEW the prompt/decoding.)_

## Summary
| Metric | Result | Target |
|---|---|---|
| Step count 5–10 | {{COUNT_OK}} | ≥18/20 |
| Clean bullet format | {{FORMAT_OK}} | ≥18/20 |
| Empty-result / refusal | {{EMPTY}} | low |
| Latency p50 | {{P50}} s | first-step <2s (see contract §5) |
| Latency p95 | {{P95}} s | — |
| Latency max | {{MAX}} s | — |

> **Note on latency:** these numbers are the *full-generation* wall time in the eval
> environment, not on-device first-step time. The app streams (contract §5), so the user
> sees step 1 well before these totals. Re-measure on a real Snapdragon device before the
> demo and record tokens/sec.

## Per-task results
| # | Steps | Count OK | Format OK | Latency (s) | State | Input |
|---|---|---|---|---|---|---|
{{TABLE}}

## Observations
- _Failure modes seen (too few/many steps, preamble, numbering, refusals):_
- _Prompt/decoding tweaks tried:_
- _Device measured on (for latency):_

## Sign-off
- [ ] AI/ML: contract met, model + tokenizer + manifest delivered to `mobile/src/models/overwhelm/`
- [ ] CTO: ExecuTorch version matches the `react-native-executorch` pin (contract §6)
- [ ] Mobile: parsing produces 5–10 clean steps from these outputs
