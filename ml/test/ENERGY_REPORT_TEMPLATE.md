# Energy Predictor — Model Report

Date: {{DATE}} · Model: tiny 1D-CNN/TCN · ExecuTorch v0.6.0 / XNNPACK · Generated by `energy_eval.py`

## Verdict: {{VERDICT}}
_(PASS = all shape & range OK, plausible ≥ N−1, and latency p95 < 50 ms. Else REVIEW.)_

## Summary
| Metric | Result | Target |
|---|---|---|
| Output shape `[1,24]` | {{SHAPE_OK}} | all |
| Values in `[0,100]` | {{RANGE_OK}} | all |
| Curve plausible (non-flat, daytime peak, overnight low) | {{PLAUSIBLE}} | ≥ N−1 |
| Latency p50 | {{P50}} ms | < 50 ms |
| Latency p95 | {{P95}} ms | < 50 ms |
| Latency max | {{MAX}} ms | < 50 ms |

> **Latency note:** measured with the ExecuTorch pybind runtime in the eval environment.
> The model is tiny (< 200 KB) so on a real Snapdragon CPU via XNNPACK this is expected to
> be sub-millisecond to a few ms. Re-measure on a target device before the demo.

## Per-scenario results
| Scenario | Shape | Range | Plausible | Overall | Peak h | Dip h | Latency (ms) | Expectation |
|---|---|---|---|---|---|---|---|---|
{{TABLE}}

## Observations
- _Curve shapes vs. expectation (lark/owl, sleep-debt flattening, weekend shift):_
- _Any flat / clipped / inverted curves:_
- _Quant scheme used (fp32 default / int8 PT2E) + size:_
- _Device measured on (for latency):_

## Sign-off
- [ ] AI/ML: contract met; `energy_predictor.pte` + `manifest.json` (with frozen scaler) delivered to `ml/models/energy/` and `mobile/src/models/energy/`
- [ ] CTO: ExecuTorch version matches the `react-native-executorch` pin (contract §10); normalization + personalization confirmed TS-side
- [ ] Mobile: builds the `[1,12,7]` window per §3, consumes the 24-pt curve per §4, derives peak/dip/overall
- [ ] Designer: forecast viz binds the 24-pt curve + headline numbers + calibrating state
